# {{ ansible_managed }}
---
# The infra nodes that will be running the dnsaas services
{% if 'designate' in bootstrap_host_services %}
dnsaas_hosts:
{% for h in infrastructure_hosts_dict %}
  {{ h.name }}:
    ip: {{ h.ip_v4 }}
{% endfor %}
{% endif %}
