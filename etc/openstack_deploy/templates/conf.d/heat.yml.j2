# {{ ansible_managed }}
---
# The infra nodes that will be running the orchestration services
{% if 'heat' in bootstrap_host_services %}
orchestration_hosts:
{% for h in infrastructure_hosts_dict %}
  {{ h.name }}:
    ip: {{ h.ip_v4 }}
{% endfor %}
{% endif %}
