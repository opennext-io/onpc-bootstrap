# {{ ansible_managed }}
---
# COE (magnum) services hosts
{% if 'magnum' in onpc_services_list %}
magnum-infra_hosts:
{% for h in controller_hosts_dict %}
  {{ h.name }}:
    ip: {{ h.ip_v4 }}
{% endfor %}
{% endif %}